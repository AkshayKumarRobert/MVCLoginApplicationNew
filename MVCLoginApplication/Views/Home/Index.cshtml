@model MVCLoginApplication.Models.LoginInfo

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <form action="/" method="post">
        <div>
            <label>UserName: @Html.TextBoxFor(x => x.UserName)</label>
            @*<input type="text" name="UserName" />*@<br />
            <label>Password: @Html.TextBoxFor(x => x.Password)</label>
            @*<input type="text" name="Password" />*@<br />
            <button>Submit</button>
        </div>
    </form>
</body>
</html>
